# Repo-wide. Each app (frontend/, and backend/ once it's back) keeps its own
# .gitignore for its build output.

node_modules/
.env
.env*.local
.DS_Store
Thumbs.db
.vercel

# Untracked for now — the Node.js backend scaffold, while the team settles
# on its approach. Still on disk; remove this line (and re-add the files)
# to bring it back into the repo.
/backend/

# AI coding-agent artifacts: useful locally, not meant to be published.
# CLAUDE.md and AGENTS.md are regenerated by `next dev`/`next build` (Next's
# agentRules feature) inside frontend/; the roadmap docs are working notes
# from building this with Claude Code.
CLAUDE.md
AGENTS.md
/frondend-roadmap
/webapp.md
